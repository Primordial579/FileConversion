services:
  - type: web
    name: rust-file-converter
    env: rust
    plan: free
    buildCommand: cargo build --release
    startCommand: cargo run
    envVars:
      - key: RUST_LOG
        value: info
